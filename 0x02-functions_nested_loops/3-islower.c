#include "main.h"

/**
 * _islower - checks for lower case character
 * @c: check character
 * Return: 1 if c is lowercase, or 0 for otherwise
 */
int _islower(int c)

{
	return (c >= 'a' && c <= 'z');
}
