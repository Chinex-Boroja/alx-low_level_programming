#include "main.h"

/**
 * main - checks function that prints if an integer is postive or negative
 * Return: 0 for success
 */

int main(void)
{
	int i;

	i = 0;
	positive_or_negative(i);

	return (0);
}
