#include <stdio.h>

/**
 * main - prints all single digit number of base 10 from 0
 * Return: 0
 */
int main(void)
{
	int n;

	for (n = 0; n < 10; n++)
		printf("%d", n);
	print("\n");
	return (0);
}
